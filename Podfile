# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'NewsFeedApp' do
  # Enable dynamic frameworks (keep this for Swift pods)
  use_frameworks!

  # Main app dependencies
  pod 'Alamofire'
  pod 'IQKeyboardManagerSwift'
  pod 'SDWebImage'

  # Unit Tests
  target 'NewsFeedAppTests' do
    inherit! :search_paths
    # Add test-specific dependencies here if needed
  end
end
  # UI Tests
  target 'NewsFeedAppUITests' do
    inherit! :search_paths
    # Add UI test dependencies if needed
 end

